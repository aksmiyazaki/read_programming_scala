package chapter14

// Remove the comments and compile to see.
//object Doubler {
//  def double(seq: Seq[String]): Seq[Int] = double(seq map (_.toInt))
//  def double(seq: Seq[Int]): Seq[Int] = seq map (_*2)
//}


object ErasureExample {

}
